<h1>Burgers!</h1>

<img src="./assets/img/burgerImage.png" alt="Burger Image" height="100" width="100">

<h2>Burgers to devour!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers' list 👉🏻</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/devoredBurger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bur">Burger Name:</label>
    <input type="text" id="bur" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
